name: supabase_feature
description: Supabase integration package for Stock Pro
version: 0.0.1
publish_to: 'none'

resolution: workspace

environment:
  sdk: ^3.11.5

dependencies:
  flutter:
    sdk: flutter
  get_it: ^9.2.1
  injectable: ^3.0.0
  supabase_flutter: ^2.8.0

dev_dependencies:
  flutter_test:
    sdk: flutter
  build_runner: ^2.4.15
  injectable_generator: ^3.0.2
  very_good_analysis: ^10.0.0