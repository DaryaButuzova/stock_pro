import 'dart:async';

import 'package:injectable/injectable.dart';

class RegistrationModule extends MicroPackageModule {
  @override
  FutureOr<void> init(GetItHelper gh) {
    // Register services, repositories, use cases here
  }
}
